# Backend Service

This service contains the NestJS backend application.

## Running the service

To run the service in development mode, run the following command:

```bash
npm run start:dev
```

## Building the service

To build the service for production, run the following command:

```bash
npm run build
```
