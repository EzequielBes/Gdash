# Worker Service

This service contains the Go worker application.

## Running the service

To run the service, run the following command:

```bash
go run cmd/main.go
```

## Building the service

To build the service for production, run the following command:

```bash
go build -o worker cmd/main.go
```